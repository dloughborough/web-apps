<%
    contacts = Contact.all
%>

<h1> Contacts </h1>

<ul>
    <% for contact in contacts %>
    <li> <%= contact.first_name %> <%= contact.last_name %> (<%=contact.company.name%>) - <%= contact.email %> - <%= contact.phone_number %> </li>
    <% end %>
</ul>